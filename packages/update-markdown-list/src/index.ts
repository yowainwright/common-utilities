/**
 * NAME
 * ----
 * DESCRIPTION
 * ----
 * @param {array}
 * @returns {item} returns the first item of an array
 */
export const updateMarkdownList = ([first]: unknown[]): unknown => first

export default updateMarkdownList
