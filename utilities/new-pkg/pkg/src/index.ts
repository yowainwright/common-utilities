/**
 * NAME
 * ----
 * DESCRIPTION
 * ----
 * @param {array}
 * @returns {item} returns the first item of an array
 */
export const name = ([first]: unknown[]): unknown => first;

export default name;
